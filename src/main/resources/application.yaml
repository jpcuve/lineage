spring:
  datasource:
    url: jdbc:mysql://10.0.0.20/lineage
    username: darts
    password: target
    driver-class-name: com.mysql.cj.jdbc.Driver
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    com:
      messio:
        lineage: info
    org:
      hibernate:
        SQL: info
